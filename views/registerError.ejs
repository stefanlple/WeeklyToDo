<!DOCTYPE html>
<html>
    <head> 
        <title>Register</title>
    </head>
    <body>
        <h1>Register</h1>
        <br>
        <h4>Account</h4>
        <p style="color:red"><%= errorMessage %> </p>
        <form action="/registration" method="POST">
        <p>Username:</p> 
        <input type="text" name="name" placeholder="Username" required>
        <br>
        <p>Password:</p> 
        <input type="password" name="password" placeholder="Password"  pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}" required>
        <br>
        <p>Reapeat Password:</p> 
        <input type="password" name="repassword" placeholder="Reapeat the password" pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}" required>
        <br>
        <button type="submit">Submit</button>
        </form>
        <p>
            The Password must contain:
             <br>-min 8 & max 16 character
             <br>-min one uppercase und lower case letter
             <br>-min one number
        </p>
        <hr>
        <p>
            If you already have an account, sign in:
            <br>
            <a href="/login"> [Login] </a> 
        </p>
        <a href="/begruessung"><img src="register.png" alt="Calendar Logo"></a>
    </body>
</html>